// Secrets.swift.example — safe to commit; contains no real credentials.
// Copy this file to Secrets.swift and fill in your values.
enum Secrets {
    static let kaggleUsername = "<your-kaggle-username>"
    static let kaggleApiKey   = "<your-kaggle-api-key>"
}
